# mainnetの「開発者が確認した身元不明のつながる可能性のあるノード」リスト(DESIGN.md §11参照)。
#
# PyBitmessage公式のDEFAULT_NODES(peer_manager.cにハードコードされたseed一覧)は
# 2026-08-21時点で全9件が到達不能だった。新規インストールでは他にmainnetへ最初の1本を
# 繋ぐ手段が無い(addr伝播もOBJECT_ONIONPEER発見も、既に1本繋がっていることが前提の
# 仕組みのため)ことへの対策として、このファイルを用意した。
#
# 重要な注意: ここに載っているノードは、このプロジェクトのメンテナが2026-08-21に
# 実際にTCP接続してP2Pプロトコルのversion/verackハンドシェイクまで完了させ、生存を
# 直接確認したというだけの情報である。運営者の身元は不明であり、いつまで稼働し続ける
# かの保証も無い(6年前のknownnodes.datバックアップから抽出した候補4425件を全件スキャン
# した結果、生存していたのはこの3件のみだった)。「身元不明の申告リストは信用しない」
# という方針(PyBitmessageのGitHub issue #2310を参考にした、addPeerに関する検討時の
# 判断)とは別枠として扱うため、peer_manager.cのハードコードされた公式seed一覧とは
# 独立したこのファイルに分離し、source='observed_seed'という別ラベルでpeers.dbへ
# 登録する(bm_peer_manager_record_resultによるrating評価・§11のクリーンアップの対象には
# 公式seedと同様に含まれる。優先的に信用するわけではなく、あくまで初回接続の足がかり
# 候補が1つ増えるだけの位置づけ)。
#
# フォーマット: 1行に "ip_address port"。#で始まる行・空行は無視する。
158.69.63.42 8080
85.114.135.102 8444
185.158.248.216 8444
